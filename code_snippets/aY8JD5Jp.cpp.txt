#include <iostream>
#include <cmath>
 
using namespace std;
 
int main() {
    //task1
    /*double x;
 
    cin >> x;
 
    cout << "y = ";
    if (x < 0) {
        cout << pow(x, 2) << endl;
    }
    else if (x <= 12.5) {
        cout << exp(x) + 5 + cos(0.001 * x) << endl;
    }
    else {
        cout << pow(x, 2) - 3 + 2.5 * pow(x, 2) << endl;
    }
    system("pause");*/
 
    //task2
    /*char symbol;
    cin >> symbol;
 
    cout << ((symbol == '.') || (symbol == ',') || (symbol == '!') || (symbol == '?') || (symbol == ';') ? "YES" : "NO") << endl;
 
    system("pause");*/
 
    return 0;
}